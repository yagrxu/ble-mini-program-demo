.terraform/
.terraform.lock.hcl

# Local state — bootstrap project, no remote backend yet.
*.tfstate
*.tfstate.backup

*.tfplan

crash.log
crash.*.log

override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Real values — keep secrets out of git.
terraform.tfvars
